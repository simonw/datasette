{
  "name": "datasette",
  "private": true,
  "devDependencies": {
    "prettier": "^2.2.1"
  },
  "scripts": {
    "fix": "npm run prettier -- --write",
    "prettier": "prettier 'datasette/static/*[!.min].js'"
  }
}
